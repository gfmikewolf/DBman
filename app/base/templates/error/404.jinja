{# app/base/templates/error/404.jinja #}
{# 后端渲染需要的参数：
    msg: 错误信息
#}
{% extends "base.jinja" %}

{% block title %}
    {{ _("Illegal access") }}
{% endblock title %}

{% block navbar %}
{% endblock navbar %}

{% block content %}
    <div class="container d-flex flex-column justify-content-center text-center">
        <div class="row mt-5 text-center">
            <p><h1>{{ _("Illegal access") }}</h1></p>
            <p class="lead">{{ msg }}</p>
            <p><a href="{{ url_for('base.index') }}" class="btn btn-primary mt-6">{{ _("homepage") }}</a></p>
        </div>
        
    </div>
{% endblock content %}
